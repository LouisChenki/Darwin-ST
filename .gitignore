# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
# 注: CLAUDE.md 现在是长期维护的项目指南(非 per-session), 故跟踪它; AGENTS.md 仍由 launcher 生成
AGENTS.md

# Experimental code/artifacts
dev/

# 冻结的实验资产 (db/日志/算子快照, 体积大且为原始证据; git 只留小型摘要)
frozen-runs/

# Results file
results.tsv

#others
.DS_Store
reference/

# P3-b 建图 + 质检产物 (运行时生成; 保留 README)
src/darwin_st/knowledge/data/*.json
src/darwin_st/knowledge/data/*.md
!src/darwin_st/knowledge/data/README.md

#Python
.venv
# 模型权重二进制 (体积大, 留服务器数据盘; git 只存档基因型/超参/模型卡/算子代码)
leaderboard/models/**/model.pt
